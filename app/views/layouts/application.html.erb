<!DOCTYPE html>
<html>
<head>
	<title><%= title %></title>
	<%= csrf_meta_tag %> <!-- special security-related tag prevents ... -->
	<%= stylesheet_link_tag 'blueprint/screen', :media => 'screen' %> <!-- stylesheet_link_tag auto appends .css to path-->
	<%= stylesheet_link_tag 'blueprint/print', :media => 'print' %>
	</head>
	<body>
		<%= yield %> <!-- the content of the page-specific view -->
	</body>
</html>